// Runtime config — generated by docker-entrypoint.sh from $API_PUBLIC_URL.
// Empty API_URL means "same origin" (/api/v1 on the same host, via Caddy).
window.__ENV__ = window.__ENV__ || { API_URL: '' };
